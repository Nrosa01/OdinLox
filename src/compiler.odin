package main

compile :: proc(source: string) {
    init_scanner(source)
    
//    for {
//        token: Token = scan_token()
//
//        if (token.line != line) {
//            fmt.fprintf("%4d ", token.line)
//            line = token.line
//        } else {
//            fmt.fprintf("   | ")
//        }
//        fmt.fprintf("%2d '%.*s'\n", token.type, token.length, token.start)
//
//        if (token.type == .TOKEN_EOF) {
//            break
//        }
//    }
}
